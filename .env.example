# Password used to log into /admin
ADMIN_PASSWORD=changeme123

# Random long random string, used to sign the admin session cookie.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=replace-with-a-long-random-string

# Where contact form submissions should be emailed (optional, not wired up yet — see README)
CONTACT_NOTIFY_EMAIL=you@example.com
